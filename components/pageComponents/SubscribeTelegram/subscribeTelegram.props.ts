export default interface SubscribeTelegramProps {
    subscribeTelegramBlock?: {
        title: string;
        description: string;
        buttonText: string;
        buttonLink: string;
        imgSrc: string;
    };
}
